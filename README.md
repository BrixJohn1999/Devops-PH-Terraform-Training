# Devops-PH-Terraform-Training

## Remove Terraform .exe

`git rm -r --cached .terraform/`
`git commit -m "Remove .terraform files from repository"`
`git push origin main`

## Add gitignore

`*.terraform*`
`*.tfstate*`

## Folder Path Listing

`tree -a`
